{
  "name": "process-tracking-handler",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "clean": "rimraf dist bundle.zip",
    "build": "tsc",
    "prepare-zip": "npm run build && zip -r bundle.zip dist/* node_modules"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.129",
    "@types/node": "^20.19.22",
    "rimraf": "^5.0.5",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.913.0"
  }
}
